#include <bits/stdc++.h>

using namespace std;

#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl "\n"

typedef long long ll;
typedef pair<int, int> pii;


int main(){	_
	int n, k;
	cin >> n >> k;
	pii aux;
	
	vector <pii > v;
	for(int i=0; i<n; i++){
		cin >> aux;
		v.push_back(aux);
	}


	exit(0);
}
