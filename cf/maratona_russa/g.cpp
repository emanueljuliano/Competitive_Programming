#include <bits/stdc++.h>

using namespace std;

#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl '\n'
#define pb push_back

typedef long long ll;


int main(){ _
	int n; cin >> n;
	map<int> mp, ma, mb;
	vector< vector<int>> m;
	for(int i=0; i<n; i++){
		int a;
		cin >> a;
		mp[a]++;
	}
	for(int i=0; i<n; i++){
		int a;
		cin >> a;
		mp[a]++;
	}
	for(int i=0; i<n; i++){
		int a;
		cin >> a;
		mp[a]++;
	}




	exit(0);
}
