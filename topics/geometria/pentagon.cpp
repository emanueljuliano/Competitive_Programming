#include <bits/stdc++.h>

using namespace std;

#define sz(x) (int)(x.size())
#define pb push_back
#define s second
#define f first
#define _ ios_base::sync_with_stdio(0);cin.tie(0);
#define endl "\n"
#define sq(x) ((x)*(x))

typedef long long ll;
typedef pair<int, int> ii;
typedef long double ld;

const int INF = 0x3f3f3f3f;
const ld eps = 1e-9;

int main(){ //_
	ld l;
	cout << setprecision(10) << fixed;
	while(cin >> l){
		cout << l << endl;
		// 2*x^2 + (l^2)/4 = 2*l² + 2*l²*cos(720/5)
		cout << x << endl;
	}
	exit(0);
}
